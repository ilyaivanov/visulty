export const zIndexes = {
  leftSidebar: 150,
  rightSidebar: 150,
  footer: 175,
  header: 125,
  modalWindow: 250,

  // is used directly from css, because there is no JS at that moment
  //so CHANGE THIS always with changing index.html inline styles
  loadingScreen: 500,
};
